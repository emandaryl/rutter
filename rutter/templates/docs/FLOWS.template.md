# Flows

<!-- Conditional — only created if the project has a real end-user journey (an app, not a pure
library/CLI/backend service with no UI). Replace every [FILL: ...] with real content read from the
repo's actual navigation/routing and screen/state definitions. Delete this comment when done. -->

## Startup

[FILL: what happens from launch/first request to the user reaching a usable state — auth check,
onboarding gate, initial data load, routing decision.]

## Core Flow(s)

[FILL: the primary flow(s) a user goes through to get value from the product — one subsection per
major flow, each as a short numbered sequence of screens/states/transitions, read from the actual
navigation/routing code.]

## Key Transitions

[FILL: notable branch points or edge-case transitions worth documenting explicitly — e.g. what
happens on session expiry, offline mode, a permission denial — only the ones that actually exist
in the code, not a hypothetical exhaustive list.]

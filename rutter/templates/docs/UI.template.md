# UI

<!-- Conditional — only created if a real UI/design-system layer was found. Replace every
[FILL: ...] with real content read from the repo's actual theme/component setup. Delete this
comment when done. -->

## Design System

[FILL: the theming approach — a theme provider/config file, design tokens (color/spacing/type
scale), and where they're defined.]

## Shared Components

[FILL: table or list of the reusable UI components already established in the codebase, each with
a one-line "when to use it" — read from the actual components directory, not invented. This is
the "check before creating" reference for Coding Conventions in AGENTS.md.]

## Conventions

[FILL: real UI conventions in use — preview/story requirement if one exists (see AGENTS.md Coding
Conventions), responsive/accessibility patterns actually followed, animation/interaction
conventions.]

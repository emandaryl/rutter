<!-- Shared body for all four tool adapters — CLAUDE.md, GEMINI.md, .github/
copilot-instructions.md, and .cursor/rules/project.mdc. Only [FILL: ToolName] changes per file
(Claude Code, Gemini CLI, GitHub Copilot, Cursor). For .cursor/rules/project.mdc specifically,
prepend Cursor's required MDC frontmatter before this body — see SKILL.md step 6 — so Cursor
actually loads it as an active rule. Delete this comment when done. -->

# [FILL: ToolName] Instructions

Read `AGENTS.md` at the repo root before doing anything else — it is the source of truth for
this project. This file is intentionally just a pointer, so there is nothing here to drift out
of sync.

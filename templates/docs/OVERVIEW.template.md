# Overview

<!-- Always created. What the project is, at a level deeper than AGENTS.md's one-liner but still
a fast orientation read — not a full spec. Replace every [FILL: ...] with real content read from
the repo (README, package structure, entry points). Delete this comment when done. -->

## What This Is

[FILL: 2-4 sentences — what the project does, who it's for, and the platform(s) it runs on.]

## Feature Map

[FILL: bullet list of the major feature areas found in the codebase (top-level screens/modules/
routes), one line each. This becomes the seed list for `.agent/features/` as those get formalized
over time — see AGENTS.md's "Creating a Feature / Reference / Task".]

## User Journey

[FILL: the primary path a user takes through the product, start to finish, in a few sentences or
a short numbered list — e.g. "sign up → onboarding → core loop → result". Omit this section
entirely if the project has no end-user journey (a library, a CLI tool, an internal service).]

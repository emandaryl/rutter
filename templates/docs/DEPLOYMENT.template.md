# Deployment

<!-- Conditional — only created if CI/CD or release config was found (a CI workflow file, fastlane
config, Dockerfile, deploy scripts, app-store metadata). Replace every [FILL: ...] with real
content read from the repo's actual config. Delete this comment when done. -->

## Environments

[FILL: the real environments the project deploys to (dev/staging/prod, or e.g. internal/beta/
production app tracks), read from actual config/branch/workflow names — not assumed.]

## Pipeline

[FILL: what triggers a deploy (a branch push, a tag, a manual dispatch) and the real steps the
pipeline runs, read from the actual CI/CD config file(s).]

## Release Process

[FILL: how a release actually ships — build artifact type, signing/versioning process, where it's
published (app store, package registry, container registry) — only what's evidenced in the repo.]

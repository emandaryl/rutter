# Setup

<!-- Conditional — only created if the repo has a real install/run path to document (a lockfile,
build scripts, env config templates, a Makefile/task runner). Replace every [FILL: ...] with the
actual commands and files found. Delete this comment when done. -->

## Prerequisites

[FILL: language/runtime versions, package manager, platform SDKs, any accounts/API keys needed —
read from the actual manifest/lockfile and any `.env.example`-style file.]

## Install

[FILL: the real install command(s) — e.g. `npm install`, `bundle install`, `./gradlew build`,
`flutter pub get`, `pod install` — as found in the repo, not a generic guess.]

## Run

[FILL: the real command(s) to run the project locally — dev server, emulator/simulator launch,
`npm run dev`, etc. — plus any required environment variables and where they're configured.]

## Common Issues

[FILL: only if the repo already documents known setup gotchas (a README troubleshooting section,
a CONTRIBUTING doc) — otherwise omit this section rather than inventing issues.]

# API

<!-- Conditional — only created if the repo exposes a REST/GraphQL/RPC surface. Replace every
[FILL: ...] with real content read from the repo's actual route/schema/controller definitions.
Delete this comment when done. -->

## Surface

[FILL: the API style (REST, GraphQL, gRPC, etc.), base URL/path convention, and where route/schema
definitions live in the codebase.]

## Auth

[FILL: how API requests are authenticated — header/token scheme — cross-reference `.agent/docs/
SECURITY.md` if that doc exists rather than duplicating it.]

## Key Endpoints / Operations

[FILL: table or list of the most important endpoints/operations, each with a one-line purpose —
not an exhaustive API reference, just enough for an agent to know where to look for a given
capability.]

## Conventions

[FILL: real conventions observed — request/response shape, error format, versioning approach,
pagination pattern.]

# Testing

<!-- Conditional — only created if a real test suite/framework was found. Replace every
[FILL: ...] with real content read from the repo's actual test setup. Delete this comment when
done. -->

## Framework & Commands

[FILL: the test framework(s) in use (Jest, JUnit, XCTest, pytest, `flutter test`, etc.) and the
real commands to run the full suite, a single file, and a single test.]

## Test Layout

[FILL: where tests live relative to source (colocated `*.test.ts`, a parallel `test/` tree,
platform-specific split like `androidTest`/`test`), and any naming convention observed.]

## Conventions

[FILL: only conventions actually visible in existing tests — e.g. a shared test-fixture/factory
pattern, a mocking convention, a golden-file/snapshot convention. Don't prescribe conventions the
codebase doesn't already follow.]
